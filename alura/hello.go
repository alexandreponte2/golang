package main

import "fmt"

func main(){
	fmt.
}






Running go list failed with go: warning: all matched no packages

Check if you can run go list -e -f {{.Name}};{{.ImportPath}};{{.Dir}} std all in a terminal successfully.